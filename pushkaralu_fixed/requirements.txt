# ── Godavari Pushkaralu 2027 — v8.0 Requirements ──────────────────────────
# FIXES vs v7:
#   - Added python-jose[cryptography]  (was missing — auth crashed on startup)
#   - Added passlib[bcrypt]            (was missing — password verify crashed)
#   - Added aioboto3                   (was missing — S3 upload crashed)

# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0

# Redis (cache + pub/sub + streams)
redis[asyncio]==5.0.4

# Async PostgreSQL
asyncpg==0.29.0

# JWT Authentication (Task 1) — WAS MISSING IN v7
python-jose[cryptography]==3.3.0

# Password hashing (Task 1) — WAS MISSING IN v7
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# S3 / Cloudflare R2 async object storage (Task 3) — WAS MISSING IN v7
aioboto3==12.4.0

# Utilities
python-dotenv==1.0.1
psutil>=5.9

# Computer Vision (CCTV worker image ONLY — do NOT install on API image)
# ultralytics>=8.2
# opencv-python-headless>=4.9